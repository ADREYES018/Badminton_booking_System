# Fresh build output
_fresh/
vite.config.ts.timestamp*

# Secrets — never commit
.env
.env.local
*.key

# Local Deno KV database
# Anchored to the repository root: a bare `data/` also matches `lib/data/`,
# which silently excluded the entire data layer from version control.
*.kv
*.kv-shm
*.kv-wal
/data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Deps
node_modules/

# Stitch design mockups — reference material, not source
stitch_smash_club_sports_utility/
